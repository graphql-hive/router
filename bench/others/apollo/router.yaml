# $ ./router --supergraph ../bench/supergraph.graphql --config router.yaml
#
# sandbox:
#   enabled: true
# homepage:
#   enabled: false
supergraph:
  path: /graphql
  listen: "0.0.0.0:4000"
traffic_shaping:
  all:
    deduplicate_query: true
  # introspection: true
# plugins:
#   # Enable with the header, Apollo-Expose-Query-Plan: true
#   experimental.expose_query_plan: true
